// import { TableConfig } from '../types';
// export const fixTableConfig = (config: TableConfig<any>) => {
//   // 兼容老的存储前端团队的 libXxx 用法
//   if (config.libTableComponentProps) {
//     config.arcoTableProps = config.libTableComponentProps;
//   }
//   if (config.globalColumnConfig?.libColumnComponentProps) {
//     config.globalColumnConfig.arcoColumnProps = config.globalColumnConfig.libColumnComponentProps;
//   }
//   config.columns?.forEach(item => {
//     if (item && item?.libColumnComponentProps) {
//       item.arcoColumnProps = item.libColumnComponentProps;
//     }
//   });
// };
//# sourceMappingURL=fixTableConfig.js.map