"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const race_scheduler_1 = require("./race-scheduler");
const safe_queue_race_1 = require("./safe-queue-race");
const safe_race_1 = require("./safe-race");
const react_hooks_1 = require("@testing-library/react-hooks");
const fakeFetcher = (expectValue, wait, failed) => new Promise((resolve, reject) => {
    setTimeout(() => {
        if (failed) {
            reject(new Error('error'));
        }
        else {
            resolve(expectValue);
        }
    }, wait);
});
const wait = (time) => new Promise(resolve => {
    setTimeout(() => {
        resolve();
    }, time);
});
function generateRandomNumber() {
    return Math.floor(Math.random() * (1000 - 100 + 1)) + 200;
}
describe('safeRace 测试', () => {
    test('仅调用一次亦正常响应', () => __awaiter(void 0, void 0, void 0, function* () {
        const safeFetcher = (0, safe_race_1.safeRace)(fakeFetcher);
        const data = yield safeFetcher(1, 100);
        expect(data).toBe(1);
    }));
    test('常规多次调用后正常响应', () => __awaiter(void 0, void 0, void 0, function* () {
        const safeFetcher = (0, safe_race_1.safeRace)(fakeFetcher);
        const callback = jest.fn();
        yield Promise.race(Array.from({ length: 10 }).map((_, i) => safeFetcher(i, generateRandomNumber()).then(callback)));
        // 调用10次，只会响应 1 次，并且响应数据是最后一次调用应该返回的数据
        expect(callback).toBeCalledTimes(1);
        expect(callback).toBeCalledWith(9);
        yield Promise.race(Array.from({ length: 5 }).map((_, i) => safeFetcher(i, generateRandomNumber()).then(callback)));
        // 再来5次调用
        expect(callback).toBeCalledTimes(2);
        expect(callback).toBeCalledWith(4);
    }));
    test('在多次调用后，响应前再次调用，应为后者产生的数据', () => __awaiter(void 0, void 0, void 0, function* () {
        const firstCallGroupCallback = jest.fn();
        const safeFetcher = (0, safe_race_1.safeRace)(fakeFetcher);
        const promises = Array.from({ length: 3 }).map((_, i) => {
            const wait = generateRandomNumber();
            return [safeFetcher(i, wait).then(firstCallGroupCallback), wait];
        });
        const lastWait = promises.map(([, wait]) => wait).at(-1);
        yield wait(lastWait - 100);
        const promiseInstances = promises.map(([p]) => p);
        promiseInstances.push(safeFetcher('last', 100));
        const data = yield Promise.race(promiseInstances);
        expect(data).toBe('last');
    }));
    test('仅最后一次调用发生异常能感知', () => __awaiter(void 0, void 0, void 0, function* () {
        const callback = jest.fn();
        const onError = jest.fn();
        const safeFetcher = (0, safe_race_1.safeRace)(fakeFetcher);
        yield Promise.race(Array.from({ length: 3 }).map((_, i) => safeFetcher(i, generateRandomNumber(), i === 1)
            .then(callback)
            .catch(onError)));
        expect(callback).toBeCalledWith(2);
        expect(onError).not.toBeCalled();
        yield Promise.race(Array.from({ length: 2 }).map((_, i) => safeFetcher(i, generateRandomNumber(), i === 1)
            .then(callback)
            .catch(onError)));
        expect(callback).toBeCalledTimes(1);
        expect(onError).toBeCalled();
    }));
    test('支持传入 null 和 undefined', () => {
        const safeFetcher = (0, safe_race_1.safeRace)(null);
        const safeFetcher2 = (0, safe_race_1.safeRace)(undefined);
        expect(safeFetcher).toBe(null);
        expect(safeFetcher2).toBe(undefined);
        const nullHook = (0, react_hooks_1.renderHook)(() => (0, safe_race_1.useSafeRace)(null));
        expect(nullHook.result.current).toBe(null);
        const hook = (0, react_hooks_1.renderHook)(() => (0, safe_race_1.useSafeRace)(fakeFetcher, { enabled: false }));
        expect(hook.result.current === fakeFetcher).toBe(true);
    });
    test('支持禁用', () => {
        const safeFetcher = (0, safe_race_1.safeRace)(fakeFetcher, { enabled: false });
        const safeFetcher2 = (0, safe_race_1.safeRace)(fakeFetcher);
        expect(safeFetcher === fakeFetcher).toBe(true);
        expect(safeFetcher2 === fakeFetcher).toBe(false);
    });
    test('测试多次调用可能不返回 Promise 的情况，前面的promise不影响立即返回的结果', () => __awaiter(void 0, void 0, void 0, function* () {
        const safeFetcher = (0, safe_race_1.safeRace)((num) => {
            if (num > 0) {
                return fakeFetcher(num, 1e3);
            }
            return num;
        });
        void safeFetcher(1);
        const z = safeFetcher(0);
        yield wait(1e3);
        expect(z).toBe(0);
    }));
    test('返回 null 之后等值', () => __awaiter(void 0, void 0, void 0, function* () {
        const safeFetcher = (0, safe_race_1.safeRace)((num) => num);
        safeFetcher(null);
        safeFetcher(undefined);
        void safeFetcher(fakeFetcher(0, 200));
        const a = safeFetcher(fakeFetcher(1, 100));
        yield wait(300);
        expect(yield a).toBe(1);
    }));
});
describe('raceScheduler 测试', () => {
    test('高低任务优先级常规测试', () => __awaiter(void 0, void 0, void 0, function* () {
        // eslint-disable-next-line @typescript-eslint/no-unsafe-return -- 安全
        const callback = jest.fn(v => v);
        const { runWithPolling, runWithSearch } = (0, race_scheduler_1.raceScheduler)({
            // 模拟轮询场景
            runWithPolling: { asyncFn: fakeFetcher, priority: 1 },
            // 模拟用户搜索场景
            runWithSearch: { asyncFn: fakeFetcher, priority: 100 },
        });
        const pollingPromises = Array.from({ length: 2 }).map((_, i) => runWithPolling('polling', 500).then(callback));
        yield wait(100);
        // 优先级更高
        const searchPromises = Array.from({ length: 2 }).map((_, i) => runWithSearch('search' + i, 100).then(callback));
        const data = yield Promise.race([...pollingPromises, ...searchPromises]);
        expect(data).toBe('search1');
        expect(callback).toBeCalledTimes(1);
    }));
    test('异常被抛出', () => __awaiter(void 0, void 0, void 0, function* () {
        const { runWithPolling, runWithSearch } = (0, race_scheduler_1.raceScheduler)({
            // 模拟轮询场景
            runWithPolling: { asyncFn: fakeFetcher, priority: 1 },
            // 模拟用户搜索场景
            runWithSearch: { asyncFn: fakeFetcher, priority: 100 },
        });
        const onError = jest.fn();
        const pollingPromises = Array.from({ length: 2 }).map((_, i) => runWithPolling('polling', 500));
        // 优先级更高
        const searchPromises = Array.from({ length: 2 }).map((_, i) => runWithSearch('search' + i, 100, i === 1).catch(onError));
        const data = yield Promise.race([...pollingPromises, ...searchPromises]);
        expect(data).toBeUndefined();
        expect(onError).toBeCalledTimes(1);
    }));
    test('多任务交叉覆盖', () => __awaiter(void 0, void 0, void 0, function* () {
        const { fetch1, fetch2, fetch3 } = (0, race_scheduler_1.raceScheduler)({
            fetch1: { asyncFn: fakeFetcher, priority: 1 },
            fetch2: { asyncFn: fakeFetcher, priority: 2 },
            fetch3: { asyncFn: fakeFetcher, priority: 3 },
        });
        let fetch1Promise = fetch1('1', 300);
        let fetch2Promise = fetch2('2', 200);
        let fetch3Promise = fetch3('3', 100);
        // 预期不生效
        fetch1Promise = fetch1('11', 50);
        fetch2Promise = fetch2('22', 50);
        fetch3Promise = fetch3('33', 50);
        const data = yield Promise.race([fetch1Promise, fetch2Promise, fetch3Promise]);
        expect(data).toBe('33');
    }));
    test('promise 被丢弃后抛出异常', () => __awaiter(void 0, void 0, void 0, function* () {
        const { fetch1, fetch2 } = (0, race_scheduler_1.raceScheduler)({
            fetch1: { asyncFn: fakeFetcher, priority: 1 },
            fetch2: { asyncFn: fakeFetcher, priority: 2 },
        }, { discardedPromiseAction: 'throw-error' });
        const onError = jest.fn();
        const p = fetch2('ok');
        yield fetch1().catch(onError);
        expect((0, race_scheduler_1.isPromiseDiscardedError)(onError.mock.calls[0][0])).toBe(true);
        yield expect(p).resolves.toBe('ok');
    }));
});
describe('safeQueueRace 测试', () => {
    test('basic use', () => __awaiter(void 0, void 0, void 0, function* () {
        const safeQueueFetcher = (0, safe_queue_race_1.safeQueueRace)(fakeFetcher);
        const values = [];
        yield Promise.all([
            safeQueueFetcher(1, 1000).then(v => values.push(v)),
            safeQueueFetcher(2, 500).then(v => values.push(v)),
            safeQueueFetcher(3, 100).then(v => values.push(v)),
        ]);
        expect(values).toEqual([1, 2, 3]);
        values.length = 0;
        yield Promise.all([
            safeQueueFetcher(4, 100).then(v => values.push(v)),
            safeQueueFetcher(5, 0).then(v => values.push(v)),
        ]);
        expect(values).toEqual([4, 5]);
    }));
    test('error catch', () => __awaiter(void 0, void 0, void 0, function* () {
        const safeQueueFetcher = (0, safe_queue_race_1.safeQueueRace)(fakeFetcher);
        const values = [];
        yield Promise.all([
            safeQueueFetcher(1, 1000).then(v => {
                values.push(v);
            }),
            safeQueueFetcher(2, 500, true)
                .then(v => {
                values.push(v);
            })
                .catch(() => {
                values.push(-1);
            }),
            safeQueueFetcher(3, 100, false).then(v => {
                values.push(v);
            }),
        ]);
        expect(values).toEqual([1, -1, 3]);
        values.length = 0;
        yield Promise.all([
            safeQueueFetcher(4, 200).then(v => {
                values.push(v);
            }),
            safeQueueFetcher(5, 0, true)
                .then(v => {
                values.push(v);
            })
                .catch(() => {
                values.push(-2);
            }),
        ]);
        expect(values).toEqual([4, -2]);
    }));
    test('error be throw', () => __awaiter(void 0, void 0, void 0, function* () {
        const safeQueueFetcher = (0, safe_queue_race_1.safeQueueRace)(fakeFetcher);
        const handleCatch = jest.fn();
        yield safeQueueFetcher(2, 500, true).catch(handleCatch);
        expect(handleCatch).toBeCalledWith(new Error(`error`));
    }));
    test('前一个报错不影响后面的响应', () => __awaiter(void 0, void 0, void 0, function* () {
        const safeQueueFetcher = (0, safe_queue_race_1.safeQueueRace)(fakeFetcher);
        // 先 reject
        let error = safeQueueFetcher(1, 0, true);
        let success = safeQueueFetcher(1, 100, false);
        yield Promise.all([expect(error).rejects.toEqual(new Error('error')), expect(success).resolves.toBe(1)]);
        // 后 reject
        error = safeQueueFetcher(1, 100, true);
        success = safeQueueFetcher(1, 0, false);
        yield Promise.all([expect(error).rejects.toEqual(new Error('error')), expect(success).resolves.toBe(1)]);
    }));
});
//# sourceMappingURL=safe-race.test.js.map