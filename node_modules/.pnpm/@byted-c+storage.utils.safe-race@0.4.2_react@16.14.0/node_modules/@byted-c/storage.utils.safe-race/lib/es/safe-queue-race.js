"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useSafeQueueRace = exports.safeQueueRace = void 0;
const react_1 = require("react");
/**
 * 反复调用，保证响应顺序与调用顺序保持一致
 *
 * ### ❓它是如何保证响应顺序和调用顺序保持一致的
 * 将每次调用按调用顺序串联到一个 promise 上，让即使已经 settled 的 promise 也要等待上个 promise settled 后才能拿到结果，下面是将每次调用分解后外部拿到的 promise 的伪代码
 *
 * 第1次调用拿到的 promise：
 *```ts
 * Promise
 *   .resolve()
 *   .then(() => fetchPromise1)
 * ```
 * 第2次调用拿到的 promise：
 * ```ts
 * Promise
 *   .resolve()
 *   .then(() => fetchPromise1)
 *   .then(() => fetchPromise2)
 * ```
 * 第3次调用拿到的 promise：
 * ```ts
 * Promise
 *   .resolve()
 *   .then(() => fetchPromise1)
 *   .then(() => fetchPromise2)
 *   .then(() => fetchPromise3)
 * ```
 * 这样就能保证我们的响应顺序与调用顺序一致了
 * @example
  const safeFetcher = safeQueueRace(() => Promise.resolve(data))

  // promise 的响应顺序一定按照调用顺序响应
  safeFetcher()
  safeFetcher()
 */
function safeQueueRace(fetcher) {
    let lastPromise = Promise.resolve();
    return ((...args) => {
        const x = fetcher(...args);
        /**
         * 为什么要在这里再次 catch 一下，下面的 catch 不能捕获错误吗？
         * 当后面的 promise 先于前面的 promise 执行时，此时的 catch 不会生效，导致 x 的错误不能被捕获
         * ```ts
         * // 这里是最小可复现demo
         * const p = new Promise(resolve => {
               setTimeout(resolve, 100);
           });
           const c = Promise.reject();
           p.then(() => c)
            .catch(err => console.log('@@', err));
         * ```
         * @example
         * Promise
         *  .resolve()
         *  // step1 1秒后resolve的promise
         *  .then(() => resolvePromise)
         *  // step2 立即 reject 的 promise
         *  .then(() => rejectPromise)
         *  .catch(() => {
         *    // 在这里无法捕获step2中立即抛出的异常
         *  })
         */
        x.catch(() => { });
        const currentPromise = lastPromise.then(() => x);
        lastPromise = currentPromise.catch(() => { }); // 确保错误不会中断链条
        // 这里返回的 promise 未捕获错误，这样外部也能处理错误
        return currentPromise;
    });
}
exports.safeQueueRace = safeQueueRace;
const useSafeQueueRace = (fn) => {
    const fnRef = (0, react_1.useRef)(fn);
    fnRef.current = fn;
    const [fetcher] = (0, react_1.useState)(() => safeQueueRace((...args) => fnRef.current(...args)));
    return fetcher;
};
exports.useSafeQueueRace = useSafeQueueRace;
//# sourceMappingURL=safe-queue-race.js.map
//# sourceMappingURL=safe-queue-race.js.map