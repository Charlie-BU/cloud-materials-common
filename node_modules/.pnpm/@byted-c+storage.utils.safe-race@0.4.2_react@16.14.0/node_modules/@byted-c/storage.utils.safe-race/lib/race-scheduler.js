"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.raceScheduler = exports.isPromiseDiscardedError = void 0;
const safe_race_1 = require("./safe-race");
function createDeferred() {
    let outerResolve = null, outerReject = null;
    const promise = new Promise((resolve, reject) => {
        outerResolve = resolve;
        outerReject = reject;
    });
    return [promise, outerResolve, outerReject];
}
class PromiseDiscardedError extends Error {
}
function isPromiseDiscardedError(v) {
    return v instanceof PromiseDiscardedError;
}
exports.isPromiseDiscardedError = isPromiseDiscardedError;
function raceScheduler(fetchers, { discardedPromiseAction = 'never-settled' } = {}) {
    let runningHighestPriorityFetcherOptions = null;
    return Object.fromEntries(Object.entries(fetchers).map(([fetcherName, currentFetcher]) => {
        const { asyncFn, priority } = currentFetcher;
        const safeAsyncFn = (0, safe_race_1.safeRace)(asyncFn);
        function runAsyncFn(...args) {
            return __awaiter(this, void 0, void 0, function* () {
                const [promise, resolve, reject] = createDeferred();
                /** 将本次 promise 的响应丢弃 */
                const discardResponse = () => {
                    if (discardedPromiseAction === 'throw-error') {
                        reject(new PromiseDiscardedError('The Promise has been discarded'));
                    }
                };
                if (!runningHighestPriorityFetcherOptions ||
                    // 同一个 fetcher 下面
                    fetcherName === runningHighestPriorityFetcherOptions.name ||
                    // 不同 fetcher 并且优先级更高
                    priority > runningHighestPriorityFetcherOptions.priority) {
                    const current = (runningHighestPriorityFetcherOptions = Object.assign(Object.assign({}, currentFetcher), { name: fetcherName }));
                    /**
                     * 仅对同名任务做出响应
                     * Q: 为什么同名任务下 runAsyncFn 多次调用不会导致多次响应？
                     * A: safeAsyncFn 已经 safeRace 过了，在同一个请求响应周期内，仅响应一次
                     */
                    safeAsyncFn(...args)
                        .then(data => {
                        if (current.name === (runningHighestPriorityFetcherOptions === null || runningHighestPriorityFetcherOptions === void 0 ? void 0 : runningHighestPriorityFetcherOptions.name)) {
                            resolve(data);
                        }
                        else {
                            discardResponse();
                        }
                    })
                        .catch(error => {
                        if (current.name === (runningHighestPriorityFetcherOptions === null || runningHighestPriorityFetcherOptions === void 0 ? void 0 : runningHighestPriorityFetcherOptions.name)) {
                            reject(error);
                        }
                        else {
                            discardResponse();
                        }
                    })
                        .finally(() => {
                        if (current.name === (runningHighestPriorityFetcherOptions === null || runningHighestPriorityFetcherOptions === void 0 ? void 0 : runningHighestPriorityFetcherOptions.name)) {
                            runningHighestPriorityFetcherOptions = null;
                        }
                    });
                    return promise;
                }
                discardResponse();
                return promise;
            });
        }
        return [fetcherName, runAsyncFn];
    }));
}
exports.raceScheduler = raceScheduler;
//# sourceMappingURL=race-scheduler.js.map