import type { FnPartial } from './type';
export interface SafeRaceOptions {
    /** 中途被丢弃的 Promise 抛出错误  */
    onDiscardedPromiseError?: (e: Error) => void;
    /** 开启能力，默认为true，设置为 false 时将原样返回 */
    enabled?: boolean;
}
/**
 * 创建一个安全的异步函数，多次连续调用，保证响应的结果是最后一次调用的
 *
 * @example
 * const safeFetcher = safeRace(fetcher);
 *
 */
export declare function safeRace<F extends FnPartial>(fetcher: F, { onDiscardedPromiseError, enabled }?: SafeRaceOptions): F;
export declare function useSafeRace<F extends FnPartial>(fn: F, safeRaceOptions?: SafeRaceOptions): F;
