"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.useSafeRace = exports.safeRace = void 0;
const react_1 = require("react");
const utils_1 = require("./utils");
/**
 * 创建一个安全的异步函数，多次连续调用，保证响应的结果是最后一次调用的
 *
 * @example
 * const safeFetcher = safeRace(fetcher);
 *
 */
function safeRace(fetcher, { onDiscardedPromiseError, enabled = true } = {}) {
    if (!fetcher || !enabled) {
        return fetcher;
    }
    let lastCallResult = null;
    return ((...args) => {
        const currentCallResult = fetcher(...args);
        lastCallResult = currentCallResult;
        if (!(0, utils_1.isPromise)(currentCallResult)) {
            // release memory
            lastCallResult = null;
            // 返回非promise则立即返回结果
            return currentCallResult;
        }
        return new Promise((resolve, reject) => {
            currentCallResult
                .then(data => {
                if (lastCallResult === currentCallResult) {
                    resolve(data);
                }
            })
                .catch(err => {
                if (lastCallResult === currentCallResult) {
                    reject(err);
                }
                else {
                    onDiscardedPromiseError === null || onDiscardedPromiseError === void 0 ? void 0 : onDiscardedPromiseError(err);
                }
            })
                .finally(() => {
                if (lastCallResult === currentCallResult) {
                    // release memory
                    lastCallResult = null;
                }
            });
        });
    });
}
exports.safeRace = safeRace;
function useSafeRace(fn, safeRaceOptions = {}) {
    const { enabled = true } = safeRaceOptions;
    const fnRef = (0, react_1.useRef)(fn);
    fnRef.current = fn;
    const [fetcher] = (0, react_1.useState)(() => safeRace((...args) => __awaiter(this, void 0, void 0, function* () { var _a; return (_a = fnRef.current) === null || _a === void 0 ? void 0 : _a.call(fnRef, ...args); }), safeRaceOptions));
    return !enabled || !fn ? fn : fetcher;
}
exports.useSafeRace = useSafeRace;
//# sourceMappingURL=safe-race.js.map