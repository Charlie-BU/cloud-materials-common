import type { AsyncFn } from './type';
export interface FetcherOptions {
    asyncFn: AsyncFn;
    priority: number;
}
export interface RaceSchedulerOptions {
    /** 被丢弃的 promise 处理策略：永不解决/抛出特定异常 */
    discardedPromiseAction?: 'never-settled' | 'throw-error';
}
declare class PromiseDiscardedError extends Error {
}
export declare function isPromiseDiscardedError(v: unknown): v is PromiseDiscardedError;
export declare function raceScheduler<T extends {
    [key: string]: FetcherOptions;
}>(fetchers: T, { discardedPromiseAction }?: RaceSchedulerOptions): {
    [K in keyof T]: T[K]['asyncFn'];
};
export {};
