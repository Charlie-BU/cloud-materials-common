import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconLockComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-lock${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M35 15c0-6.075-4.925-11-11-11S13 8.925 13 15v4H6a2 2 0 00-2 2v21a2 2 0 002 2h36a2 2 0 002-2V21a2 2 0 00-2-2h-7v-4zM8 40V23h32v17H8zm17-14a1 1 0 011 1v9a1 1 0 01-1 1h-2a1 1 0 01-1-1v-9a1 1 0 011-1h2zm-8-11a7 7 0 1114 0v4H17v-4z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconLock = React.forwardRef(IconLockComponent);
export default IconLock;
