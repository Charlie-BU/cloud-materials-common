import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconDoorComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-door${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M16 26v-5a1 1 0 011-1h2a1 1 0 011 1v5a1 1 0 01-1 1h-2a1 1 0 01-1-1zM9.762 2h28.476C39.211 2 40 2.658 40 3.47v41.06c0 .812-.789 1.47-1.762 1.47H9.762C8.789 46 8 45.342 8 44.53V3.47C8 2.659 8.789 2 9.762 2zM12 6v36h24V6H12z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconDoor = React.forwardRef(IconDoorComponent);
export default IconDoor;
