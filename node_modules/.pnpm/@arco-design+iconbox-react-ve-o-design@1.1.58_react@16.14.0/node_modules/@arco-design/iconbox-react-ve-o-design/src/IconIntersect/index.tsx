import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconIntersectComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-intersect${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M46 5a2 2 0 00-2-2H16a2 2 0 00-2 2v9H4a2 2 0 00-2 2v26a2 2 0 002 2h28a2 2 0 002-2v-9h10a2 2 0 002-2V5zM6 18h8v13a2 2 0 002 2h14v7H6V18zm24 0H18v11h12V18zM18 7h24v22h-8V16a2 2 0 00-2-2H18V7z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconIntersect = React.forwardRef(IconIntersectComponent);
export default IconIntersect;
