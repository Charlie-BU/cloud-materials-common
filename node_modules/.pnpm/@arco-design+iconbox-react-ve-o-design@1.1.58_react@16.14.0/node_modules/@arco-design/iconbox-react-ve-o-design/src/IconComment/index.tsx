import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconCommentComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-comment${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M42 3a2 2 0 012 2v32a2 2 0 01-2 2H31.777l-6.44 5.796a2 2 0 01-2.675 0L16.222 39H6a2 2 0 01-2-2V5a2 2 0 012-2h36zm-2 4H8v28h9.757L24 40.618 30.242 35H40V7zm-6 13a1 1 0 00-1-1H15a1 1 0 00-1 1v2a1 1 0 001 1h18a1 1 0 001-1v-2z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconComment = React.forwardRef(IconCommentComponent);
export default IconComment;
