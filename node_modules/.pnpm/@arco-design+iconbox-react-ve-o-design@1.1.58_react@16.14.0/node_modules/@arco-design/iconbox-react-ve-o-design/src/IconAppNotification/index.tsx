import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconAppNotificationComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-app-notification${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M36 20a8 8 0 110-16 8 8 0 010 16zm2 2.5a.5.5 0 01.5-.5h3a.5.5 0 01.5.5V42a2 2 0 01-2 2H6a2 2 0 01-2-2V8a2 2 0 012-2h19.5a.5.5 0 01.5.5v3a.5.5 0 01-.5.5H8v30h29a1 1 0 001-1V22.5zM40 12a4 4 0 11-8 0 4 4 0 018 0z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconAppNotification = React.forwardRef(IconAppNotificationComponent);
export default IconAppNotification;
