import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconPolygonComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-polygon${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M42.182 17.677L26.464 5.387a4 4 0 00-4.928 0L5.818 17.678a4 4 0 00-1.322 4.443l5.981 17.542a4 4 0 003.786 2.71h19.474a4 4 0 003.786-2.71l5.981-17.542a4 4 0 00-1.322-4.443zm-33.9 3.152L24 8.539l15.718 12.29-5.981 17.542H14.263L8.281 20.83z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconPolygon = React.forwardRef(IconPolygonComponent);
export default IconPolygon;
