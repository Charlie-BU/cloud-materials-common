import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconLampComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-lamp${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M38.147 4.824A1 1 0 0037.163 4H10.837a1 1 0 00-.984.824l-4.643 26A1 1 0 006.194 32H22v9h-7a1 1 0 00-1 1v2a1 1 0 001 1h18a1 1 0 001-1v-2a1 1 0 00-1-1h-7v-9h15.807a1 1 0 00.984-1.176l-4.643-26zM13.35 8H34.65l3.571 20H9.778l3.57-20z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconLamp = React.forwardRef(IconLampComponent);
export default IconLamp;
