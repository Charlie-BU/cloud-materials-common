import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconWalletComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-wallet${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M43.684 6C44.964 6 46 6.848 46 7.895v32.21C46 41.152 44.963 42 43.684 42H4.316C3.036 42 2 41.152 2 40.105V7.895C2 6.848 3.037 6 4.316 6h39.368zM42 10H6v28h36v-6h-8a8 8 0 110-16h8v-6zm0 10h-8a4 4 0 000 8h8v-8zm-8 2a2 2 0 110 4 2 2 0 010-4z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconWallet = React.forwardRef(IconWalletComponent);
export default IconWallet;
