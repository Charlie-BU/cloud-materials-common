import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconToRightComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-to-right${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M43 4a1 1 0 011 1v38a1 1 0 01-1 1h-2a1 1 0 01-1-1V5a1 1 0 011-1h2zM26.4 14.618a1.4 1.4 0 01.904.332l9.433 7.981a1.4 1.4 0 010 2.138l-9.433 7.981A1.4 1.4 0 0125 31.982L24.999 26H5a1 1 0 01-1-1v-2a1 1 0 011-1h20v-5.982a1.4 1.4 0 011.4-1.4z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconToRight = React.forwardRef(IconToRightComponent);
export default IconToRight;
