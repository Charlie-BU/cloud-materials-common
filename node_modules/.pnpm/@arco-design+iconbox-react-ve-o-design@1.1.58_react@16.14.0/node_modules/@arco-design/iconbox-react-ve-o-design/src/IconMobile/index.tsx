import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconMobileComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-mobile${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M38 2c1.105 0 2 .9 2 2v40c0 1.1-.895 2-2 2H10c-1.105 0-2-.9-2-2V4c0-1.1.895-2 2-2h28zm-2 4H12v36h24V6zM24 33a3 3 0 110 6 3 3 0 010-6zm3-22l.117.007A1 1 0 0128 12v2l-.007.117A1 1 0 0127 15h-6l-.117-.007A1 1 0 0120 14v-2l.007-.117A1 1 0 0121 11h6z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconMobile = React.forwardRef(IconMobileComponent);
export default IconMobile;
