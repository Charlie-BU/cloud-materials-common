import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconDimensionComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-dimension${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M24 5a8 8 0 012 15.748v4.143l6.838 4.276a8 8 0 11-2.83 2.945l-6.009-3.755-6.006 3.754a8 8 0 11-2.83-2.945L22 24.893v-4.144A8 8 0 0124 5zM11 32a4 4 0 100 8 4 4 0 000-8zm26 0a4 4 0 100 8 4 4 0 000-8zM24 9a4 4 0 100 8 4 4 0 000-8z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconDimension = React.forwardRef(IconDimensionComponent);
export default IconDimension;
