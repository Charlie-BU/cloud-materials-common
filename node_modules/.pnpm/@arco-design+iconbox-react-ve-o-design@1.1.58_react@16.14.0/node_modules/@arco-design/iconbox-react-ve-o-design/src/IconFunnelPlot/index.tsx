import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconFunnelPlotComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-funnel-plot${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M46.25 7a1 1 0 00-1-1H2.75a1 1 0 00-.86 1.508l21.249 36.031a1 1 0 001.722 0l21.25-36.031A1 1 0 0046.249 7zM18.618 28H29.38L24 37.126 18.619 28zm16.07-9H13.31l2.95 5h15.48l2.948-5zM8.002 10h31.994l-2.95 5H10.953l-2.95-5z" fill={useCurrentColor ? 'currentColor' : '#000'} fillOpacity=".85"/></svg>;
}

const IconFunnelPlot = React.forwardRef(IconFunnelPlotComponent);
export default IconFunnelPlot;
