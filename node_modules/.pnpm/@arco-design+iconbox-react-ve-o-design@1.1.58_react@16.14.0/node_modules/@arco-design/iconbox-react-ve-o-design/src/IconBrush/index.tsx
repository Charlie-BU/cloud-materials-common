import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconBrushComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-brush${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M35 16v4a2 2 0 01-2 2H7a2 2 0 01-2-2V8a2 2 0 012-2h26a2 2 0 012 2v4h7a1 1 0 011 1v15.122a1 1 0 01-.87.992L20 32v10a1 1 0 01-1 1h-2a1 1 0 01-1-1V29.554a1 1 0 01.87-.992L39 25.676V16h-4zM9 10v8h22v-8H9z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconBrush = React.forwardRef(IconBrushComponent);
export default IconBrush;
