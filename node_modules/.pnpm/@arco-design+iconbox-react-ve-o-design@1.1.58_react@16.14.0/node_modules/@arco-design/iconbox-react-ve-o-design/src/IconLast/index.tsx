import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconLastComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-last${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M40 6.003v35.994c0 1.648-1.882 2.588-3.2 1.6L12.8 25.6a2 2 0 010-3.2l24-17.996c1.319-.99 3.2-.049 3.2 1.6zm-29-2a1 1 0 011 1v37.993a1 1 0 01-1 1H9a1 1 0 01-1-1V5.004a1 1 0 011-1h2zm25 6L17.333 24 36 37.998V10.003z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconLast = React.forwardRef(IconLastComponent);
export default IconLast;
