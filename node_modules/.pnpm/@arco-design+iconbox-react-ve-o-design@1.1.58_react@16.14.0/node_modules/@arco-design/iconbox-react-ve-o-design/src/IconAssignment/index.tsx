import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconAssignmentComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-assignment${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M8 45h32a2 2 0 002-2V6a2 2 0 00-2-2h-8v4h6v26h-6a1 1 0 00-1 1v6H10V8h18V4H8a2 2 0 00-2 2v37a2 2 0 002 2zm7-30a1 1 0 00-1 1v2a1 1 0 001 1h18a1 1 0 001-1v-2a1 1 0 00-1-1H15zm-1 11a1 1 0 011-1h14a1 1 0 011 1v2a1 1 0 01-1 1H15a1 1 0 01-1-1v-2z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconAssignment = React.forwardRef(IconAssignmentComponent);
export default IconAssignment;
