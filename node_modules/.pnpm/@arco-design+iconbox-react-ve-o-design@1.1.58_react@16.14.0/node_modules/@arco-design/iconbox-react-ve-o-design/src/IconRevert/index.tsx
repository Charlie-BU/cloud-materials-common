import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconRevertComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-revert${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M11.172 12H40v28H9V24H5v18a2 2 0 002 2h35a2 2 0 002-2V10a2 2 0 00-2-2H11.172l2.95-2.95a1 1 0 000-1.414l-1.415-1.414a1 1 0 00-1.414 0l-7.071 7.07a1 1 0 000 1.415l7.07 7.071a1 1 0 001.415 0l1.414-1.414a1 1 0 000-1.414L11.171 12z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconRevert = React.forwardRef(IconRevertComponent);
export default IconRevert;
