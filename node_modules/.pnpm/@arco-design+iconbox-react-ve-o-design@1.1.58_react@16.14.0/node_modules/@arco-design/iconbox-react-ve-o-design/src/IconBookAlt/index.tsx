import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconBookAltComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-book-alt${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M38 15a1 1 0 011-1h2a1 1 0 011 1v29a2 2 0 01-2 2H8a2 2 0 01-2-2V5a3 3 0 012.824-2.995L9 2h23a2 2 0 012 2v26H10v12h28V15zm-5 19a1 1 0 011 1v2a1 1 0 01-1 1H15a1 1 0 01-1-1v-2a1 1 0 011-1h18zM30 6H10v20h20V6z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconBookAlt = React.forwardRef(IconBookAltComponent);
export default IconBookAlt;
