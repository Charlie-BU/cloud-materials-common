import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconSortComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-sort${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path d="M14.38 28.456a.278.278 0 01.213-.456h18.814c.235 0 .364.275.213.456l-9.108 10.93a.667.667 0 01-1.024 0l-9.108-10.93zM14.38 19.544a.278.278 0 00.213.456h18.814a.278.278 0 00.213-.456l-9.108-10.93a.667.667 0 00-1.024 0l-9.108 10.93z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconSort = React.forwardRef(IconSortComponent);
export default IconSort;
