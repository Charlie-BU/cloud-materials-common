import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconMorphComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-morph${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M44.09 7.913l-40-1.818A2 2 0 002 8.093v31.814a2 2 0 002.09 1.998l40-1.818A2 2 0 0046 38.089V9.91a2 2 0 00-1.91-1.998zM6 37.813V10.187l36 1.636v24.356L6 37.814z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconMorph = React.forwardRef(IconMorphComponent);
export default IconMorph;
