import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconDevProcessComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-dev-process${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M37 2a7 7 0 11-6.71 9.001L15.5 11a5.5 5.5 0 100 11h17a9.5 9.5 0 010 19l-14.79.001a7 7 0 110-4.001H32.5a5.5 5.5 0 100-11h-17a9.5 9.5 0 010-19h14.79c.86-2.892 3.539-5 6.71-5zM11 36a3 3 0 100 6 3 3 0 000-6zM34 9a3 3 0 116 0 3 3 0 01-6 0z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconDevProcess = React.forwardRef(IconDevProcessComponent);
export default IconDevProcess;
