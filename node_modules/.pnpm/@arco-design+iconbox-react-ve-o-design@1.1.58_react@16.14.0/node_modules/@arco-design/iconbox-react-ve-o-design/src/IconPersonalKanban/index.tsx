import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconPersonalKanbanComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-personal-kanban${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M6 7h36a2 2 0 012 2v30a2 2 0 01-2 2H6a2 2 0 01-2-2V9a2 2 0 012-2zm2 4v26h32V11H8zm17 13a1 1 0 011 1v7a1 1 0 01-1 1h-3a1 1 0 01-1-1v-7a1 1 0 011-1h3zm9-6a1 1 0 011 1v13a1 1 0 01-1 1h-3a1 1 0 01-1-1V19a1 1 0 011-1h3zm-17-7v8a1 1 0 01-1 1H8v-9h9z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconPersonalKanban = React.forwardRef(IconPersonalKanbanComponent);
export default IconPersonalKanban;
