import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconToLeftComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-to-left${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M7 4a1 1 0 011 1v38a1 1 0 01-1 1H5a1 1 0 01-1-1V5a1 1 0 011-1h2zm15.669 11.114a1.4 1.4 0 01.331.904V22h20a1 1 0 011 1v2a1 1 0 01-1 1H23v5.981a1.4 1.4 0 01-2.304 1.07l-9.433-7.982a1.4 1.4 0 010-2.138l9.433-7.981a1.4 1.4 0 011.973.164z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconToLeft = React.forwardRef(IconToLeftComponent);
export default IconToLeft;
