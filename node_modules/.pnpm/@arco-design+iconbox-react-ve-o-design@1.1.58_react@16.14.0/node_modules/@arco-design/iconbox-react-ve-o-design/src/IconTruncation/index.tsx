import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconTruncationComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-truncation${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M6 5a1 1 0 011-1h2a1 1 0 011 1v38a1 1 0 01-1 1H7a1 1 0 01-1-1V5zm14 18a1 1 0 011-1h17V5a1 1 0 011-1h2a1 1 0 011 1v38a1 1 0 01-1 1h-2a1 1 0 01-1-1V26H21a1 1 0 01-1-1v-2z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconTruncation = React.forwardRef(IconTruncationComponent);
export default IconTruncation;
