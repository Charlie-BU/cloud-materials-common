import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconSeparateWindowComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-separate-window${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M26 20V10h16v10H26zM22 8c0-1.105.9-2 2-2h20c1.1 0 2 .895 2 2v14c0 1.105-.9 2-2 2H24c-1.1 0-2-.895-2-2V8zM4 10h14v4H5.976v24h32.043V28H42v12c0 1.105-.886 2-2.003 2H4c-1.096 0-2-.895-2-2V12c0-1.105.873-2 2-2z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconSeparateWindow = React.forwardRef(IconSeparateWindowComponent);
export default IconSeparateWindow;
