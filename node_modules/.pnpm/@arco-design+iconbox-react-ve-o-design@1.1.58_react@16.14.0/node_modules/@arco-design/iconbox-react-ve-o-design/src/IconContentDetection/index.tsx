import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconContentDetectionComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-content-detection${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M8 32v8h8v4H6a2 2 0 01-2-2V32h4zm33.339-5.425l2.828 2.829-14.142 14.142-8.485-8.485 2.828-2.829 5.657 5.657L41.34 26.575zM16 4v4H8v8H4V6a2 2 0 012-2h10zm26 0a2 2 0 012 2v10h-4V8h-8V4h10z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconContentDetection = React.forwardRef(IconContentDetectionComponent);
export default IconContentDetection;
