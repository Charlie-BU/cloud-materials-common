import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconSubordinatesComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-subordinates${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M20 3a1 1 0 011 1v16a1 1 0 01-1 1h-8v15.5h16V28a1 1 0 011-1h16a1 1 0 011 1v16a1 1 0 01-1 1H29a1 1 0 01-1-1v-3.5H8V21H4a1 1 0 01-1-1V4a1 1 0 011-1h16zm22 28H32v10h10V31zM7 7h10v10H7V7z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconSubordinates = React.forwardRef(IconSubordinatesComponent);
export default IconSubordinates;
