import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconHideSeparateWindowComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-hide-separate-window${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M44 6a2 2 0 012 2v32a2 2 0 01-2 2H4a2 2 0 01-2-2V8a2 2 0 012-2h40zm-26 4H6v28h36V26H20.333C19.05 26 18 25.006 18 23.778V10zm4 0h20v12H22V10z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconHideSeparateWindow = React.forwardRef(IconHideSeparateWindowComponent);
export default IconHideSeparateWindow;
