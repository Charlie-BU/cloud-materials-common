import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconLampAltComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-lamp-alt${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M37.147 2.824A1 1 0 0036.163 2H11.837a1 1 0 00-.984.824l-4.643 26A1 1 0 007.194 30H22v12h-7a1 1 0 00-1 1v2a1 1 0 001 1h18a1 1 0 001-1v-2a1 1 0 00-1-1h-7V30h4v6a1 1 0 001 1h2a1 1 0 001-1v-6h6.807a1 1 0 00.984-1.176l-4.643-26zM14.35 6H33.65l3.571 20H10.777L14.35 6z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconLampAlt = React.forwardRef(IconLampAltComponent);
export default IconLampAlt;
