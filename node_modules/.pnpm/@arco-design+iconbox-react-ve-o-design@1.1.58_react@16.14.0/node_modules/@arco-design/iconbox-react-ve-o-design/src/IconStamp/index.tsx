import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconStampComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-stamp${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M35 3a1 1 0 011 1v9a1 1 0 01-1 1h-4.5l-.75 6H45a1 1 0 011 1v15a1 1 0 01-1 1h-2v7a1 1 0 01-1 1H6a1 1 0 01-1-1v-7H3a1 1 0 01-1-1V21a1 1 0 011-1h15.249l-.75-6H13a1 1 0 01-1-1V4a1 1 0 011-1h22zm4 34H9v4h30v-4zM6 24h36v9H6v-9zm20.468-10h-4.937l.75 6h3.438l.749-6zM16 7h16v3H16V7z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconStamp = React.forwardRef(IconStampComponent);
export default IconStamp;
