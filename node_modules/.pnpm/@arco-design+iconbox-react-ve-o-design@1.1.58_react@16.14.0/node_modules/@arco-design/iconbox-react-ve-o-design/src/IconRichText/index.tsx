import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconRichTextComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-rich-text${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M7.003 28a1 1 0 011 1v11h11.008a1 1 0 011 1v2a1 1 0 01-1 1H6a2 2 0 01-2-2V29a1 1 0 011-1h2.003zM42 4a2 2 0 012 2v13a1 1 0 01-1 1h-2a1 1 0 01-1-1V8H29a1 1 0 01-1-1V5a1 1 0 011-1h13z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconRichText = React.forwardRef(IconRichTextComponent);
export default IconRichText;
