import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconChairComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-chair${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M37 2a1 1 0 011 1v22h4a1 1 0 011 1v2a1 1 0 01-1 1h-2.001L40 45a1 1 0 01-1 1h-2a1 1 0 01-1-1l-.001-10H12v10a1 1 0 01-1 1H9a1 1 0 01-1-1V29H6a1 1 0 01-1-1v-2a1 1 0 011-1h4V3a1 1 0 011-1h26zm-1.001 27H12v2h23.999v-2zM34 12H14v13h20V12zm0-6H14v2h20V6z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconChair = React.forwardRef(IconChairComponent);
export default IconChair;
