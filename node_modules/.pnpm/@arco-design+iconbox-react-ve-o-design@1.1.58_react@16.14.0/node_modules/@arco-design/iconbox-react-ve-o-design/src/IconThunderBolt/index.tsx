import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconThunderBoltComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-thunder-bolt${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M27 8.35L12.809 23.79l10.169 1.937V38.92l13.434-16.178L27 21.29V8.35zm4 9.51l9.51 1.468a2 2 0 011.235 3.254L22.516 45.738a2 2 0 01-3.538-1.277V29.037L8.53 27.047a2 2 0 01-1.098-3.317L27.527 1.866A2 2 0 0131 3.219V17.86z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconThunderBolt = React.forwardRef(IconThunderBoltComponent);
export default IconThunderBolt;
