import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconP4Component(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = false, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-P4${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path d="M3.73926 6.04663L7.99139 6.04673 12.2603 6.04663M3.73926 9.95312L7.99139 9.95348 12.2603 9.95312" stroke="color(display-p3 .6 .4 0)" strokeWidth="1.5" strokeLinecap="round"/></svg>;
}

const IconP4 = React.forwardRef(IconP4Component);
export default IconP4;
