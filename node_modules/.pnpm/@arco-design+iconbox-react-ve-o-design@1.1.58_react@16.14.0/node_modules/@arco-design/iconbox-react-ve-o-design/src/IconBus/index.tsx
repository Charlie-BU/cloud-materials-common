import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconBusComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-bus${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M40 4a4 4 0 014 4v26a4 4 0 01-4 4v3a3 3 0 01-3 3h-6a3 3 0 01-3-3v-3h-8v3a3 3 0 01-3 3h-6a3 3 0 01-3-3v-3a4 4 0 01-4-4V8a4 4 0 014-4h32zM16 38h-4v2h4v-2zm16 0h4v2h-4v-2zm8-14H8v3h9v4H8v3h32v-3h-9v-4h9v-3zM8 8h32v12H8V8z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconBus = React.forwardRef(IconBusComponent);
export default IconBus;
