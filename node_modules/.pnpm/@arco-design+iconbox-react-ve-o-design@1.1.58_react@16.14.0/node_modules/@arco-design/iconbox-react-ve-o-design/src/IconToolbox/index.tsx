import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconToolboxComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-toolbox${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M33 6a2 2 0 012 2v4h9a2 2 0 012 2v26a2 2 0 01-2 2H4a2 2 0 01-2-2V14a2 2 0 012-2h9V8a2 2 0 012-2h18zM17 26H6v12h36V26H31a7 7 0 11-14 0zm7 3a3 3 0 01-3-3h6a3 3 0 01-3 3zm18-13H6v6h36v-6zm-25-6h14v2H17v-2z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconToolbox = React.forwardRef(IconToolboxComponent);
export default IconToolbox;
