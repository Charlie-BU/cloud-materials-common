import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconPowerSwitchComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-power-switch${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M43 4a1 1 0 011 1v37a2 2 0 01-2 2H6a2 2 0 01-2-2V5a1 1 0 011-1h38zm-3 4H8v32h32V8zm-3 4a1 1 0 00-1-1H12a1 1 0 00-1 1v23a2 2 0 002 2h22a2 2 0 002-2V12zm-3.5 2.5h-19v19h19v-19zM30 18a1 1 0 00-1-1H19a1 1 0 00-1 1v1.5a1 1 0 001 1h10a1 1 0 001-1V18z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconPowerSwitch = React.forwardRef(IconPowerSwitchComponent);
export default IconPowerSwitch;
