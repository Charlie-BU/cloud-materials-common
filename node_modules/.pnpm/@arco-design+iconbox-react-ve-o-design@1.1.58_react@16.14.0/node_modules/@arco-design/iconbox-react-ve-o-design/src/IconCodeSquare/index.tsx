import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconCodeSquareComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-code-square${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M42 6a2 2 0 00-2-2H8a2 2 0 00-2 2v36a2 2 0 002 2h32a2 2 0 002-2V6zM10 8h28v32H10V8zm20.268 9l3.864 1.035L29.99 33.49l-3.864-1.035L30.268 17zm-5.954 2.121l-2.829-2.828L13 24.778l.318.318-.025.025L21.07 32.9 23.9 30.07l-5.268-5.268 5.683-5.682z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconCodeSquare = React.forwardRef(IconCodeSquareComponent);
export default IconCodeSquare;
