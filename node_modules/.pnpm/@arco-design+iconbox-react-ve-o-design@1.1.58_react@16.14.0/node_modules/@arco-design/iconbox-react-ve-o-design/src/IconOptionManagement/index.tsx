import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconOptionManagementComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-option-management${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M43 5a2 2 0 012 2v34a2 2 0 01-2 2H5a2 2 0 01-2-2V7a2 2 0 012-2h38zm-2 4h-4v7.234a1 1 0 01-1.514.857L32 15l-3.485 2.091A1 1 0 0127 16.234V9H7v30h34V9zM22 30a1 1 0 011 1v2a1 1 0 01-1 1H12a1 1 0 01-1-1v-2a1 1 0 011-1h10zm9-8a1 1 0 011 1v2a1 1 0 01-1 1H12a1 1 0 01-1-1v-2a1 1 0 011-1h19z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconOptionManagement = React.forwardRef(IconOptionManagementComponent);
export default IconOptionManagement;
