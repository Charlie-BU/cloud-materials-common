import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconToTopComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-to-top${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M24.904 11.099c.06.05.115.105.165.164l7.981 9.433A1.4 1.4 0 0131.982 23H26v20a1 1 0 01-1 1h-2a1 1 0 01-1-1V23h-5.982a1.4 1.4 0 01-1.068-2.304l7.981-9.433a1.4 1.4 0 011.973-.164zM43 4a1 1 0 011 1v2a1 1 0 01-1 1H5a1 1 0 01-1-1V5a1 1 0 011-1h38z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconToTop = React.forwardRef(IconToTopComponent);
export default IconToTop;
