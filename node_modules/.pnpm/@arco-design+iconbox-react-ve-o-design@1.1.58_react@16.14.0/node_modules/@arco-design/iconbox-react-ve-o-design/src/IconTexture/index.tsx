import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconTextureComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-texture${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M46.276 8.807L43.448 5.98l-9.9 9.9-9.899-9.9-9.9 9.9-9.9-9.9-2.828 2.828L13.75 21.535l9.9-9.9 9.9 9.9L46.275 8.807zm0 21l-2.828-2.828-9.9 9.9-9.899-9.9-9.9 9.9-9.9-9.9-2.828 2.828L13.75 42.535l9.9-9.9 9.9 9.9 12.727-12.728z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconTexture = React.forwardRef(IconTextureComponent);
export default IconTexture;
