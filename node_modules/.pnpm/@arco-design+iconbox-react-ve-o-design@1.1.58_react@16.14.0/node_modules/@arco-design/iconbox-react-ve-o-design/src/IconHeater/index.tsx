import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconHeaterComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-heater${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M20 5a2 2 0 012 2v15h4V7a2 2 0 012-2h10a2 2 0 012 2v15h4a1 1 0 011 1v2a1 1 0 01-1 1h-4v15a2 2 0 01-2 2H28a2 2 0 01-2-2V26h-4v15a2 2 0 01-2 2H10a2 2 0 01-2-2V26H4a1 1 0 01-1-1v-2a1 1 0 011-1h4V7a2 2 0 012-2h10zm-2 4h-6v30h6V9zm12 0h6v30h-6V9z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconHeater = React.forwardRef(IconHeaterComponent);
export default IconHeater;
