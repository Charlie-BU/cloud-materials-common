import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconP1Component(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = false, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-P1${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path d="M12.2607 8.91639L8.00861 11.4275 3.73975 8.91639M12.2607 5.0099L8.00861 7.521 3.73975 5.0099" stroke="color(display-p3 .1373 .4392 .251)" strokeWidth="1.5" strokeLinecap="round"/></svg>;
}

const IconP1 = React.forwardRef(IconP1Component);
export default IconP1;
