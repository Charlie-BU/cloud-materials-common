import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconViewAltComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-view-alt${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M22 6a2 2 0 00-2-2H5a2 2 0 00-2 2v36a2 2 0 002 2h15a2 2 0 002-2V6zm23 21a2 2 0 00-2-2H27a2 2 0 00-2 2v15a2 2 0 002 2h16a2 2 0 002-2V27zM18 8H7v32h11V8zm11 21h12v11H29V29zM43 4a2 2 0 012 2v15a2 2 0 01-2 2H27a2 2 0 01-2-2V6a2 2 0 012-2h16zM29 8h12v11H29V8z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconViewAlt = React.forwardRef(IconViewAltComponent);
export default IconViewAlt;
