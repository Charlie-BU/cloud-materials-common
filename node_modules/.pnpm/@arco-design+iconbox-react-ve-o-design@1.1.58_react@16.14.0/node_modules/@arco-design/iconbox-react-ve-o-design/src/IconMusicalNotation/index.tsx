import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconMusicalNotationComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-musical-notation${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M30.137 7.167C28.81 6.25 27 7.2 27 8.812V24A9.956 9.956 0 0021 22c-5.523 0-10 4.477-10 10s4.477 10 10 10 10-4.477 10-10c0-.164-.004-.328-.012-.49V12.474l6.785 4.824 2.212-3.328-9.848-6.803zM15 32a6 6 0 1112 0 6 6 0 01-12 0z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconMusicalNotation = React.forwardRef(IconMusicalNotationComponent);
export default IconMusicalNotation;
