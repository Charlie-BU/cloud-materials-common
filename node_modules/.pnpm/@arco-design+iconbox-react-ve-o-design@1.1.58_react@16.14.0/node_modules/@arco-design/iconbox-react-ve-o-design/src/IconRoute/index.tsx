import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconRouteComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-route${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M22 30v-9H11.216a1 1 0 01-.75-1.662L23.25 4.85a1 1 0 011.5 0l12.784 14.488a1 1 0 01-.75 1.662H26v12a1 1 0 01-1 1H14v10h-4V31a1 1 0 011-1h11zm2-19.954L17.864 17h12.272L24 10.046z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconRoute = React.forwardRef(IconRouteComponent);
export default IconRoute;
