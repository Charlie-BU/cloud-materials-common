import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconAlignHorizontalCentersComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-align-horizontal-centers${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M26 5c0-.55-.45-1-1-1h-2c-.55 0-1 .45-1 1v6h-8a1 1 0 00-1 1v8a1 1 0 001 1h8v6H7a1 1 0 00-1 1v8a1 1 0 001 1h15v6c0 .55.45 1 1 1h2c.55 0 1-.45 1-1v-6h15a1 1 0 001-1v-8a1 1 0 00-1-1H26v-6h8a1 1 0 001-1v-8a1 1 0 00-1-1h-8V5z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconAlignHorizontalCenters = React.forwardRef(IconAlignHorizontalCentersComponent);
export default IconAlignHorizontalCenters;
