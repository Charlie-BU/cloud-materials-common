import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconFormulaComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-formula${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path d="M25.927 24L7.725 9.398A2.5 2.5 0 019.352 5H40a1 1 0 011 1v2a1 1 0 01-1 1H13.407l17.696 14.22a1 1 0 010 1.56L13.406 39H40a1 1 0 011 1v2a1 1 0 01-1 1H9.352a2.5 2.5 0 01-1.627-4.398L25.927 24z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconFormula = React.forwardRef(IconFormulaComponent);
export default IconFormula;
