import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconAlignBottomComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-align-bottom${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M12 34a1 1 0 01-1-1V7a1 1 0 011-1h8a1 1 0 011 1v26a1 1 0 01-1 1h-8zm-7 4c-.55 0-1 .45-1 1v2c0 .55.45 1 1 1h38c.55 0 1-.45 1-1v-2c0-.55-.45-1-1-1H5zm22-5a1 1 0 001 1h8a1 1 0 001-1V17a1 1 0 00-1-1h-8a1 1 0 00-1 1v16z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconAlignBottom = React.forwardRef(IconAlignBottomComponent);
export default IconAlignBottom;
