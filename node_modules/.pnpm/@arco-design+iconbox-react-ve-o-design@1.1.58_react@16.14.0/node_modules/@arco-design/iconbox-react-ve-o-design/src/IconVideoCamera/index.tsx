import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconVideoCameraComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-video-camera${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M3 8h28a2 2 0 011.995 1.85L33 10v28a2 2 0 01-1.85 1.995L31 40H3a2 2 0 01-1.995-1.85L1 38V10a2 2 0 011.85-1.995L3 8h28H3zm26 4H5v24h24V12zm18 3.532v16.936c0 1.554-1.865 2.514-3.332 1.715L36 30V18l7.668-4.183c1.467-.8 3.332.16 3.332 1.715zm-4 3.533l-3 1.2v7.47l3 1.2v-9.87zM15 16v6H9v-6h6z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconVideoCamera = React.forwardRef(IconVideoCameraComponent);
export default IconVideoCamera;
