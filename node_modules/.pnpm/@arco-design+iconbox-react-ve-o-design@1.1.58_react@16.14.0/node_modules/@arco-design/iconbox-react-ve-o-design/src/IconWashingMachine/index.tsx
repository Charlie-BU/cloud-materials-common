import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconWashingMachineComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-washing-machine${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M40 4a1 1 0 011 1v38a1 1 0 01-1 1H8a1 1 0 01-1-1V5a1 1 0 011-1h32zm-3 12H11v24h26V16zm-4 12a9 9 0 10-18 0 9 9 0 0018 0zm-9-5a5 5 0 100 10 5 5 0 000-10zM11 8h26v4H11V8z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconWashingMachine = React.forwardRef(IconWashingMachineComponent);
export default IconWashingMachine;
