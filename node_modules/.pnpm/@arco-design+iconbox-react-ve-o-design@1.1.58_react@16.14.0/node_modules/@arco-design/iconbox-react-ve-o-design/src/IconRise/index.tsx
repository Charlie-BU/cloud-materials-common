import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconRiseComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = false, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-rise${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 12 13" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M10.3807 8.68155C10.6426 9.00893 10.4095 9.4939 9.99027 9.4939L2.00976 9.4939C1.59051 9.4939 1.35742 9.00893 1.61933 8.68155L5.60958 3.69373C5.80974 3.44352 6.19029 3.44352 6.39045 3.69373L10.3807 8.68155Z" fill={useCurrentColor ? 'currentColor' : '#DB373F'}/></svg>;
}

const IconRise = React.forwardRef(IconRiseComponent);
export default IconRise;
