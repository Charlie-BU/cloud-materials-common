import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconLevelTestComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-level-test${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M44 41a1 1 0 011 1v2a1 1 0 01-1 1H4a1 1 0 01-1-1v-2a1 1 0 011-1h40zM9 39a2 2 0 01-2-2V21a2 2 0 012-2h8v-6a2 2 0 012-2h8V5a2 2 0 012-2h10a2 2 0 012 2v32a2 2 0 01-2 2H9zm8-16h-6v12h6V23zm10-8h-6v20h6V15zm10-8h-6v28h6V7z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconLevelTest = React.forwardRef(IconLevelTestComponent);
export default IconLevelTest;
