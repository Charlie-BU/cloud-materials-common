import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconMessageBoxComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-message-box${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M44 4a2 2 0 012 2v10a2 2 0 01-2 2h-2v24a2 2 0 01-2 2H8a2 2 0 01-2-2V18H4a2 2 0 01-2-2V6a2 2 0 012-2h40zm-6 14H10v22h28V18zm-7 4a1 1 0 011 1v2a1 1 0 01-1 1H17a1 1 0 01-1-1v-2a1 1 0 011-1h14zM42 8H6v6h36V8z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconMessageBox = React.forwardRef(IconMessageBoxComponent);
export default IconMessageBox;
