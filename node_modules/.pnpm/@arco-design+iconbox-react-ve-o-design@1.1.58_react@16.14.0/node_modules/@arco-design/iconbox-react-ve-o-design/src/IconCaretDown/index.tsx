import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconCaretDownComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-caret-down${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 12 12" fill={useCurrentColor ? 'currentColor' : '#000'} xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path d="M9.63916 4.23102C9.90849 3.88017 9.6564 3.375 9.21198 3.375L2.78802 3.375C2.3436 3.375 2.09151 3.88017 2.36084 4.23102L5.57282 8.41502C5.78775 8.69499 6.21225 8.69499 6.42718 8.41502L9.63916 4.23102Z"/></svg>;
}

const IconCaretDown = React.forwardRef(IconCaretDownComponent);
export default IconCaretDown;
