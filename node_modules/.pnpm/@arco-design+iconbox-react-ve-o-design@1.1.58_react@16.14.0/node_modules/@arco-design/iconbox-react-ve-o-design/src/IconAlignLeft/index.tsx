import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconAlignLeftComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-align-left${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M10 5c0-.55-.45-1-1-1H7c-.55 0-1 .45-1 1v38c0 .55.45 1 1 1h2c.55 0 1-.45 1-1V5zm32 31a1 1 0 01-1 1H15a1 1 0 01-1-1v-8a1 1 0 011-1h26a1 1 0 011 1v8zM31 21a1 1 0 001-1v-8a1 1 0 00-1-1H15a1 1 0 00-1 1v8a1 1 0 001 1h16z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconAlignLeft = React.forwardRef(IconAlignLeftComponent);
export default IconAlignLeft;
