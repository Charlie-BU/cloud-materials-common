import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconColorComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-color${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M40 26.604c0-6.586-5.162-13.706-15.997-21.185C13.164 12.916 8 20.038 8 26.604c0 8.836 7.163 16 16 16s16-7.164 16-16zM25.113 1.338C37.704 9.76 44 18.182 44 26.604c0 11.045-8.954 20-20 20s-20-8.955-20-20C4 18.202 10.296 9.78 22.888 1.339a2 2 0 012.225-.001z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconColor = React.forwardRef(IconColorComponent);
export default IconColor;
