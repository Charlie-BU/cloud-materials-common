import React, { ForwardedRef, useContext } from 'react';
import { OriginIconProps } from '../type';
import { Context } from '../context';

function IconNextComponent(props: OriginIconProps, ref: ForwardedRef<SVGSVGElement>) {
    const { prefix: prefixFromContext } = useContext(Context);
    const { className = '', prefix: prefixFromProps, width = '1em', height = '1em', useCurrentColor = true, spin, ...rest } = props;

    const prefix = prefixFromProps || prefixFromContext || 'force';
    const loadingKls = spin ? ` ${prefix}-icon-loading` : '';
    return <svg className={`${prefix}-icon ${prefix}-icon-next${loadingKls} ${className}`} width={width} height={height} viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" {...rest} ref={ref}><path fillRule="evenodd" clipRule="evenodd" d="M11.2 4.404l24 17.996a2 2 0 010 3.2l-24 17.996c-1.318.99-3.2.049-3.2-1.6V6.004c0-1.648 1.882-2.588 3.2-1.6zm27.8-.4a1 1 0 011 1v37.992a1 1 0 01-1 1h-2a1 1 0 01-1-1V5.004a1 1 0 011-1h2zm-27 5.998v27.996L30.667 24 12 10.002z" fill={useCurrentColor ? 'currentColor' : '#000'}/></svg>;
}

const IconNext = React.forwardRef(IconNextComponent);
export default IconNext;
