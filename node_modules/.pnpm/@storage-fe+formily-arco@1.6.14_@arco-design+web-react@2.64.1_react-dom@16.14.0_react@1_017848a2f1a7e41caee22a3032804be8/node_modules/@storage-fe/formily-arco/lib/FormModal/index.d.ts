import React from 'react';
import { IFormProps, Form } from '@formily/core';
import { ReactFC } from '@formily/react';
import { IMiddleware } from '@formily/shared';
import { ModalProps } from '@arco-design/web-react';
declare type FormModalRenderer = React.ReactElement | ((form: Form) => React.ReactElement);
declare type ModalTitle = string | number | React.ReactElement;
export interface IFormModal {
    forOpen(middleware: IMiddleware<IFormProps>): IFormModal;
    forConfirm(middleware: IMiddleware<Form>): IFormModal;
    forCancel(middleware: IMiddleware<Form>): IFormModal;
    open(props?: IFormProps): Promise<any>;
    close(): void;
}
export interface IModalProps extends ModalProps {
    loadingText?: React.ReactNode;
}
export declare function FormModal(title: IModalProps, id: string, renderer: FormModalRenderer): IFormModal;
export declare function FormModal(title: IModalProps, renderer: FormModalRenderer): IFormModal;
export declare function FormModal(title: ModalTitle, id: string, renderer: FormModalRenderer): IFormModal;
export declare function FormModal(title: ModalTitle, renderer: FormModalRenderer): IFormModal;
export declare namespace FormModal {
    var FooterLeft: ReactFC<{}>;
    var Portal: {
        (props: React.PropsWithChildren<import("../__builtins__").IPortalProps>): React.JSX.Element;
        defaultProps: {
            id: string | symbol;
        };
    };
}
export default FormModal;
