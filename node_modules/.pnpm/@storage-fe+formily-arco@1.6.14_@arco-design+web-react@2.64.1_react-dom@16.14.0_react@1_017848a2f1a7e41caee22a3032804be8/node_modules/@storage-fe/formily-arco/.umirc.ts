const path = require('path');

export default {
  mode: 'site',
  title: 'storage-formily',
  alias: {
    '@storage-fe/formily-arco': path.join(__dirname)
  },
  resolve: {
    includes: ['src', 'docs'],
  },
  navs: [{ title: 'Arco Design', path: '/formily-arco' }],
  devtool: 'source-map',
};
