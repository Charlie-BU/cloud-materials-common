import { createEdenConfig } from '@ies/eden-lib-build';
import path from 'path';

console.log('__dirname', path.join(__dirname, 'src'));

export default createEdenConfig({
  builder: 'babel',
  abilities: {
    ts: true,
    less: {
      lessLoaderOptions: {
        // 配置使用不同版本的 less(less-loader 兼容less 3 和 4)
        implementation: require.resolve('less'),
      },
    },
  },
});
