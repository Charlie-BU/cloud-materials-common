import React from "react";
import { Input, Select, FormItem } from "@storage-fe/formily-arco";
import { createForm } from "@formily/core";
import { FormProvider, Field } from "@formily/react";
import { DatePicker } from "@arco-design/web-react";

const form = createForm();

// eslint-disable-next-line max-lines-per-function
export default () => (
  <FormProvider form={form}>
    <h1>label配置</h1>
    <h3>label为空时的展示</h3>
    <Field
      name="empty-title"
      title={""}
      decorator={[FormItem]}
      component={[Input]}
    />
    <h3>冒号</h3>
    <Field
      name="default-colon"
      title="默认展示冒号"
      decorator={[FormItem]}
      component={[Input]}
    />
    <Field
      name="no-colon"
      title="无冒号(colon=false)"
      decorator={[FormItem, { colon: false }]}
      component={[DatePicker]}
    />
    <h3>固定宽度设置</h3>
    <Field
      name="fixed-width-default"
      title="固定宽度(默认)"
      decorator={[FormItem]}
      component={[Input]}
    />
    <Field
      name="auto-width-default"
      title="自动宽度"
      decorator={[FormItem, { labelWidth: "auto" }]}
      component={[Input]}
    />
    <Field
      name="fixed-width-col"
      title="固定宽度(labelCol)"
      decorator={[FormItem, { labelCol: 3 }]}
      component={[Input]}
    />
    <Field
      name="fixed-width-width"
      title="固定宽度(labelWidth)"
      decorator={[FormItem, { labelWidth: 200 }]}
      component={[Input]}
    />
    <Field
      name="fixed-width-overflow"
      title="固定宽度溢出固定宽度溢出固定宽度溢出固定宽度溢出固定宽度溢出"
      decorator={[FormItem, { labelWidth: 200 }]}
      component={[Input]}
    />
    <Field
      name="fixed-width-wrap"
      title="固定宽度换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行"
      decorator={[
        FormItem,
        { labelWidth: 150, labelWrap: true, tooltip: { content: "提示提示", position: 'bottom' } },
      ]}
      component={[Input]}
    />
    <h3>label对齐方式</h3>
    <Field
      name="align-default"
      title="默认"
      required
      decorator={[FormItem]}
      component={[Input]}
    />
    <Field
      name="align-default-left"
      title="左对齐"
      decorator={[FormItem, { labelAlign: "left" }]}
      component={[Input]}
    />
    <h3>星号展示位置</h3>
    <Field
      name="required-default"
      title="必填(星号显示前面)"
      decorator={[FormItem]}
      component={[Input]}
      required
    />
    <Field
      name="required-end"
      title="必填(星号显示后面)"
      decorator={[FormItem, { requiredSymbol: { position: "end" } }]}
      component={[Select]}
      required
    />

    <h1>布局方式</h1>
    <Field
      name="layout-horizontal"
      title={"layout(horizontal)"}
      decorator={[FormItem]}
      component={[Input]}
    />
    <Field
      name="layout-vertical"
      title={"layout(vertical)"}
      decorator={[FormItem, { layout: "vertical", labelAlign: "left" }]}
      component={[Input]}
    />

    <h1>尺寸</h1>
    <Field
      name="size(mini)"
      title={"size(mini)"}
      decorator={[FormItem, { size: "mini" }]}
      component={[Input]}
    />
    <Field
      name="size(small)"
      title={"size(small)"}
      decorator={[FormItem, { size: "small" }]}
      component={[Input]}
    />
    <Field
      name="size(default)"
      title={"size(default)"}
      decorator={[FormItem, { size: "default" }]}
      component={[Input]}
    />
    <Field
      name="size(large)"
      title={"size(large)"}
      decorator={[FormItem, { size: "large" }]}
      component={[Input]}
    />

    <h1>内容区域</h1>
    <Field
      name="error"
      title={"校验错误"}
      decorator={[
        FormItem,
        {
          validateStatus: "error",
          help: "这是一个错误信息",
          hasFeedback: true,
        },
      ]}
      component={[Input]}
    />
    <Field
      name="error-popover"
      title="校验错误(popover)"
      decorator={[
        FormItem,
        {
          validateStatus: "error",
          help: "这是一个错误信息",
          hasFeedback: true,
          helpLayout: "popover",
        },
      ]}
      component={[Input]}
    />
    <Field
      name="validation"
      title="校验中"
      decorator={[
        FormItem,
        { validateStatus: "validating", help: "校验中", hasFeedback: true },
      ]}
      component={[Input]}
    />
    <Field
      name="warning"
      title="警告"
      decorator={[
        FormItem,
        { validateStatus: "warning", help: "这是一个警告", hasFeedback: true },
      ]}
      component={[Input]}
    />
    <Field
      name="success"
      title="成功"
      decorator={[
        FormItem,
        { validateStatus: "success", help: "成功", hasFeedback: true },
      ]}
      component={[Input]}
    />
    <Field
      name="extra"
      title="额外提示信息(extra)"
      decorator={[
        FormItem,
        {
          validateStatus: "success",
          help: "成功",
          extra: "这是一个额外提示信息",
        },
      ]}
      component={[Input]}
    />
    <Field
      name="hasFeedback"
      title="显示icon(hasFeedback = true)"
      decorator={[
        FormItem,
        { validateStatus: "validating", hasFeedback: true },
      ]}
      component={[Input]}
    />

    <Field
      name="helpers"
      title="提示"
      decorator={[FormItem, {
        controlPopover: {
          helperTips: ['这是一个错误信息', '这是第二个错误信息'],
          trigger: 'hover',
          position: 'bottom',
          style: {
            backgroundColor: 'red',
          },
        },
      }]}
      component={[Input]}
      validator={[
        {
          validator: (value) => {
            if (value === 'a') {
              return '这是一个错误信息';
            }
          }
        },
        {
          validator: (value) => {
            if (value === 'b') {
              return '这是第二个错误信息';
            }
          }
        }
      ]}
    />

    <Field
      name="addon"
      title="前后缀"
      decorator={[
        FormItem,
        { addonBefore: <div>前缀</div>, addonAfter: <div>后缀</div> },
      ]}
      component={[Input]}
    />

    <Field
      name="wrapperCol"
      title="容器宽度(wrapperCol)"
      decorator={[FormItem, { wrapperCol: 10, wrapperWidth: "100px" }]}
      component={[Input]}
    />
  </FormProvider>
);
