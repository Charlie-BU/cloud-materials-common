import React from 'react';
import { Input, Select, FormItem } from '@storage-fe/formily-arco';
import { createForm } from '@formily/core';
import { FormProvider, createSchemaField } from '@formily/react';

const Title = (props: any) => <h3>{props.text}</h3>;
const SchemaField = createSchemaField({
  components: {
    Input,
    Select,
    FormItem,
    Title,
  },
});

const form = createForm();

// eslint-disable-next-line max-lines-per-function
export default () => (
  <FormProvider form={form}>
    <SchemaField>
      <SchemaField.Void x-component="Title" x-component-props={{ text: 'Tooltip' }} />
      <SchemaField.String
        title="展示tooltip"
        x-decorator="FormItem"
        x-component="Input"
        x-decorator-props={{ tooltip: { content: 'tooltip' }, labelWrap: true }}
      />

      <SchemaField.Void x-component="Title" x-component-props={{ text: '前后缀' }} />
      <SchemaField.String
        title="前缀"
        x-decorator="FormItem"
        x-component="Input"
        x-decorator-props={{ addonBefore: <div>前缀</div> }}
      />
      <SchemaField.String
        title="后缀"
        x-decorator="FormItem"
        x-component="Input"
        x-decorator-props={{ addonAfter: <div>后缀</div> }}
      />
      <SchemaField.String
        title="前后缀加校验图标"
        x-decorator="FormItem"
        x-component="Input"
        x-decorator-props={{
          addonBefore: <div>前缀</div>,
          addonAfter: <div>后缀</div>,
          validateStatus: 'validating',
          hasFeedback: true,
          help: '校验中',
        }}
      />

      <SchemaField.String
        title="换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行换行"
        x-decorator="FormItem"
        x-component="Input"
        x-decorator-props={{ labelWrap: true }}
      />

      <SchemaField.String
        title="溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出溢出"
        x-decorator="FormItem"
        x-component="Input"
      />
    </SchemaField>
  </FormProvider>
);
