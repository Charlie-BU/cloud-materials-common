import React, { useCallback } from "react";
import { Button, ButtonProps } from "@arco-design/web-react";
import { useFormStep } from "../hooks";
import { observer, useForm } from "@formily/react";

const BackButton = observer((props: ButtonProps) => {
  const formStep = useFormStep();
  const form = useForm();

  const onClick = useCallback((e) => {
    props?.onClick?.(e);
    formStep?.back();
  }, []);

  return (
    <Button
      disabled={!formStep?.allowBack || form?.submitting}
      loading={formStep?.backing}
      {...props}
      onClick={onClick}
    >
      {props.children || "上一步"}
    </Button>
  );
});

export default Object.assign(BackButton, { __BYTE_BUTTON: true });
