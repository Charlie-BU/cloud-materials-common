import { ReactChildren, ReactElement } from 'react';
/**
 * 解析 FormStep 的 JSX children，并从一级目录中，按顺序找出所有 StepPane
 * @param children
 * @returns
 */
export declare const parseJSXSteps: (children?: ReactChildren) => ReactElement<any, string | import("react").JSXElementConstructor<any>>[];
