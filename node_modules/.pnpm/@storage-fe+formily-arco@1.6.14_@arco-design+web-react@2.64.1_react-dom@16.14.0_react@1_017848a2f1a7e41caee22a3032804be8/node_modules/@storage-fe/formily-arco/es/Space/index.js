import _Object$assign from "@babel/runtime-corejs3/core-js-stable/object/assign";
import React from "react";
import { Space as ArcoSpace } from "@arco-design/web-react";
import { useFormLayout } from "../FormLayout";
import toArray from "rc-util/es/Children/toArray";
export var Space = function Space(props) {
  var _a;
  var layout = useFormLayout();
  /**
   * formily 内部组件会通过 createElement 包装一层，导致 children 包装一个 <Fragment />
   * 导致传入 ArcoSpace 的只有一个节点生成一个 SpaceItem
   */
  var childNodes = toArray(props.children, {
    keepEmpty: true
  });
  return /*#__PURE__*/React.createElement(ArcoSpace, _Object$assign(_Object$assign({
    size: (_a = props.size) !== null && _a !== void 0 ? _a : layout === null || layout === void 0 ? void 0 : layout.spaceSize
  }, props), {
    children: childNodes
  }));
};
export default Space;