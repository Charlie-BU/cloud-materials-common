import _slicedToArray from "@babel/runtime-corejs3/helpers/slicedToArray";
import _indexOfInstanceProperty from "@babel/runtime-corejs3/core-js-stable/instance/index-of";
import _Object$getOwnPropertySymbols from "@babel/runtime-corejs3/core-js-stable/object/get-own-property-symbols";
import _Object$assign from "@babel/runtime-corejs3/core-js-stable/object/assign";
var __rest = this && this.__rest || function (s, e) {
  var t = {};
  for (var p in s) {
    if (Object.prototype.hasOwnProperty.call(s, p) && _indexOfInstanceProperty(e).call(e, p) < 0) t[p] = s[p];
  }
  if (s != null && typeof _Object$getOwnPropertySymbols === "function") for (var i = 0, p = _Object$getOwnPropertySymbols(s); i < p.length; i++) {
    if (_indexOfInstanceProperty(e).call(e, p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i])) t[p[i]] = s[p[i]];
  }
  return t;
};
/**
 * 1. FormItem网格布局
 * 2. 居中，居右，居左布局
 * 3. 行内布局
 * 4. 吸底布局
 */
import React, { useRef, useLayoutEffect, useState } from 'react';
import { Space } from '@arco-design/web-react';
import { BaseItem } from '../FormItem';
import { usePrefixCls } from '../__builtins__';
import StickyBox from 'react-sticky-box';
import cls from 'classnames';
function getDefaultBackground() {
  // have to add to the document in order to use getComputedStyle
  var div = document.createElement('div');
  document.head.appendChild(div);
  var bg = window.getComputedStyle(div).backgroundColor;
  document.head.removeChild(div);
  return bg;
}
function getInheritedBackgroundColor(el) {
  // get default style for current browser
  var defaultStyle = getDefaultBackground(); // typically "rgba(0, 0, 0, 0)"
  // get computed color for el
  var _window$getComputedSt = window.getComputedStyle(el),
    backgroundColor = _window$getComputedSt.backgroundColor;
  // if we got a real value, return it
  // eslint-disable-next-line eqeqeq
  if (backgroundColor != defaultStyle) {
    return backgroundColor;
  }
  // if we've reached the top parent el without getting an explicit color, return default
  if (!el.parentElement) {
    return defaultStyle;
  }
  // otherwise, recurse and try again on parent element
  return getInheritedBackgroundColor(el.parentElement);
}
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore
export var FormButtonGroup = function FormButtonGroup(_a) {
  var align = _a.align,
    gutter = _a.gutter,
    props = __rest(_a, ["align", "gutter"]);
  var prefixCls = usePrefixCls('formily-button-group');
  return /*#__PURE__*/React.createElement(Space, _Object$assign({}, props, {
    size: gutter,
    className: cls(prefixCls, props.className),
    style: _Object$assign(_Object$assign({}, props.style), {
      justifyContent: align === 'left' ? 'flex-start' : align === 'right' ? 'flex-end' : 'center',
      display: 'flex'
    })
  }), props.children);
};
FormButtonGroup.defaultProps = {
  align: 'left'
};
var FormItem = function FormItem(_a) {
  var gutter = _a.gutter,
    props = __rest(_a, ["gutter"]);
  return /*#__PURE__*/React.createElement(BaseItem, _Object$assign({}, props, {
    label: " ",
    style: _Object$assign(_Object$assign({
      margin: 0,
      padding: 0
    }, props.style), {
      width: '100%'
    }),
    colon: false
  }), props.children ? /*#__PURE__*/React.createElement(Space, {
    size: gutter
  }, props.children) : props.children);
};
FormButtonGroup.FormItem = FormItem;
var Sticky = function Sticky(_a) {
  var align = _a.align,
    props = __rest(_a, ["align"]);
  var ref = useRef(null);
  var _useState = useState('transparent'),
    _useState2 = _slicedToArray(_useState, 2),
    color = _useState2[0],
    setColor = _useState2[1];
  var prefixCls = usePrefixCls('formily-button-group');
  useLayoutEffect(function () {
    if (ref.current) {
      var computed = getInheritedBackgroundColor(ref.current);
      if (computed !== color) {
        setColor(computed);
      }
    }
  });
  return /*#__PURE__*/React.createElement(StickyBox, _Object$assign({}, props, {
    className: cls("".concat(prefixCls, "-sticky"), props.className),
    style: _Object$assign({
      backgroundColor: color
    }, props.style),
    bottom: true
  }), /*#__PURE__*/React.createElement("div", {
    ref: ref,
    className: "".concat(prefixCls, "-sticky-inner"),
    style: _Object$assign(_Object$assign({}, props.style), {
      justifyContent: align === 'left' ? 'flex-start' : align === 'right' ? 'flex-end' : 'center'
    })
  }, props.children));
};
FormButtonGroup.Sticky = Sticky;
FormButtonGroup.Sticky.defaultProps = {
  align: 'left'
};
export default FormButtonGroup;