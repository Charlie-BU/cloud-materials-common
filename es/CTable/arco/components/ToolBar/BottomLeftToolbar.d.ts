import React from 'react';
import type { ToolbarConfig } from '../../types';
/**
 * table 底部的 toolbar 实现写得比较死，只支持左下角的操作按钮，并且不支持输入性组件，等有诉求的时候再考虑
 * */
export declare const BottomLeftToolbar: React.FC<{
    config: ToolbarConfig;
}>;
